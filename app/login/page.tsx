"use client";import{useState}from"react";import{supabaseBrowser}from"@/lib/supabase";
export default function Page(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");async function signIn(){const s=supabaseBrowser();const{error}=await s.auth.signInWithPassword({email,password});if(error)return alert(error.message);location.href="/profile"}async function signUp(){const s=supabaseBrowser();const{error}=await s.auth.signUp({email,password});if(error)return alert(error.message);alert("Account created")}return <main className="mx-auto max-w-md px-4 py-12"><div className="glass rounded-[2rem] p-6"><h1 className="text-3xl font-black">Login</h1><div className="mt-6 space-y-3"><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button onClick={signIn} className="btn w-full">Login</button><button onClick={signUp} className="btn-dark w-full">Create Account</button></div></div></main>}
